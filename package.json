{
  "name": "maira-beauty-atelier-cinematic",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html styles.css content.js motion.js dist/"
  }
}
